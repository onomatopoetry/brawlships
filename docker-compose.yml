version: '3.8'

services:
  web:
    build: .
    ports:
      - "10000:10000"
    volumes:
      - .:/app
      - /app/node_modules
    environment:
      - NODE_ENV=development
      - PORT=10000
    command: npm run dev